# Post-Auth Setup Ledger (24G231, `capture.pcapng`, `tcp.stream==2`)

## Scope

- Build: macOS Sequoia `24G231`
- Capture: `capture.pcapng`
- Stream: `tcp.stream==2`
- Canonical binary identity: [../06-tooling/binary-baseline.md](../06-tooling/binary-baseline.md)

## Summary

- `confirmed`: auth completion is followed by a short cleartext setup window before the rekey boundary.
- `confirmed`: frame `625` is `ServerInit`.
- `confirmed`: frame `631` is the viewer-info message handled by the `HandleSetEncodingsMessage` command multiplexer rooted at `screensharingd::sub_1000352ac`.
- `confirmed`: frame `638` is a `HandleViewerCutTextMessage` client packet.
- `confirmed`: frame `641` is the `EncodeEncryptionInfo` rekey packet, sent as a one-rectangle `FramebufferUpdate` with encoding `0x44f`.
- `confirmed`: the two encrypted `16`-byte blocks inside frame `641` match the Frida-captured `CCCryptorUpdate` output byte-for-byte.
- `confirmed`: frame `645` begins the first opaque post-rekey server burst on this stream.

## Binary Anchors

- `screensharingd::sub_1000352ac`
  - post-auth command multiplexer for viewer messages.
- `screensharingd::sub_100020ef8`
  - `EncodeEncryptionInfo` sender path.
- `screensharingd::sub_100016fb8`
  - `SetupAESKeys`, which recreates the local transport cryptors.

All approved offsets for these labels are tracked in [../06-tooling/binary-baseline.md](../06-tooling/binary-baseline.md).

## Frame Ledger

### Frame `625` Server -> Client, `63` bytes

- Type:
  - `confirmed`: `ServerInit`
- Parsed fields:
  - width `1440`
  - height `810`
  - pixel format `32/24`, true-color
  - name length `39`
  - name payload contains `Alex’s Mac mini`

### Frame `631` Client -> Server, `66` bytes

- Raw prefix:
  - `21 00 00 3e 00 01 00 00 00 02 00 00 00 05 00 00 ...`
- Type:
  - `confirmed`: viewer-info message
- Evidence:
  - command byte is `0x21`
  - payload length and field layout match the previously recovered viewer-info message family
  - the same run’s host log reaches the post-auth setup handlers immediately afterward

### Frame `638` Client -> Server, `16` bytes

- Raw:
  - `12 00 00 01 00 01 00 01 00 00 00 01 0a 00 00 01`
- Type:
  - `confirmed`: `HandleViewerCutTextMessage`

### Frame `641` Server -> Client, `52` bytes

- Raw:
  - `0000000100000000000000000000044f00000001069cdbf549ee21021c73ef9b29c93a924549b7fb18620ee16a66cbe85230702a`
- Type:
  - `confirmed`: `EncodeEncryptionInfo`
- Recovered layout:
  - bytes `0..15`
    - `00 00 00 01 00 00 00 00 00 00 00 00 00 00 04 4f`
    - `confirmed`: standard one-rectangle `FramebufferUpdate` header with encoding `0x44f`
  - bytes `16..19`
    - `00 00 00 01`
    - `strong-inference`: generation / sequence field from `viewer + 0x8cc`
  - bytes `20..35`
    - `06 9c db f5 49 ee 21 02 1c 73 ef 9b 29 c9 3a 92`
    - `confirmed`: ECB-wrapped next transport key block
  - bytes `36..51`
    - `45 49 b7 fb 18 62 0e e1 6a 66 cb e8 52 30 70 2a`
    - `confirmed`: ECB-wrapped companion IV block

### Frame `647` Client -> Server, `8` bytes

- Raw:
  - `12 00 00 02 00 01 00 00`
- Type:
  - `confirmed`: second `HandleViewerCutTextMessage` header packet

### Frame `645` Server -> Client, `860` bytes

- Type:
  - `confirmed`: first opaque post-rekey server record burst on this stream
- Notes:
  - outer traffic shape matches the established post-`0x44f` encrypted record layer
  - exact inner field mapping for this run remains `open` in this page

## Frida-Proven Rekey Material

The attached Frida trace for this same session captured the plaintext material entering the `0x44f` sender path:

- next transport key (`viewer + 0x8d2`):
  - `c3754936827b3c2c984f19a46625de41`
- next transport IV (`viewer + 0x8e2`):
  - `03767a819658423f8ffb351989369543`
- matching ciphertext block 1:
  - `069cdbf549ee21021c73ef9b29c93a92`
- matching ciphertext block 2:
  - `4549b7fb18620ee16a66cbe85230702a`

Result:

- `confirmed`: frame `641` is the exact on-wire `EncodeEncryptionInfo` packet generated by the hooked `CCCryptorUpdate` calls.
- `confirmed`: immediately after the packet is emitted, `screensharingd` recreates the four local AES cryptor contexts from that recovered key/IV material.

## HP Status From the Same Run

- `confirmed`: auth succeeded (`good authentication`, `Authentication: SUCCEEDED`).
- `confirmed`: `ScreensharingAgent` logged `ProMode not active` before the daemon reached `set encodings`.
- `confirmed`: the daemon crashed after `set encodings` because of the unsafe inline Frida hook, so this run cannot prove the final host-side deny branch.
- `strong-inference`: high performance was not enabled before the crash, but the exact gate reason remains unresolved.

## Current Interpretation

- `confirmed`: the active `24G231` transport model includes a clear post-auth setup window, a `0x44f` rekey packet, and then a rekeyed encrypted record layer.
- `confirmed`: the attached key/IV pair belongs to the same `capture.pcapng` session and should be treated as run-scoped evidence, not a protocol constant.
- `open`: later post-rekey frame families on this stream still need direct decode correlation if we want field-level reconstruction for this specific run.
